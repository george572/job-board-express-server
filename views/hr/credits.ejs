<!DOCTYPE html>
<html lang="ka">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include('../partials/head-meta', { seo }) %>
  <link rel="stylesheet" href="/css/style.css" />
  <style>
    .hr-sidebar-item:hover,
    .hr-sidebar-item.active {
      background: rgba(59, 130, 246, 0.08);
      color: rgba(37, 99, 235, 0.95);
    }
    .credits-option {
      cursor: pointer;
    }
    .credits-option.credits-option-selected {
      border-color: #315EFF;
      background: rgba(49, 94, 255, 0.06);
      box-shadow: 0 0 0 2px rgba(49, 94, 255, 0.2);
    }
    #credits-buy-btn.loading .credits-buy-btn-text { display: none; }
    #credits-buy-btn.loading .credits-buy-btn-spinner { position: absolute; left: 50%; top: 50%; transform: translate(-50%, -50%); }
    #credits-buy-btn { position: relative; min-width: 8rem; }
    .credits-buy-btn-spinner {
      display: none;
      width: 1.25rem;
      height: 1.25rem;
      border: 2px solid rgba(255,255,255,0.3);
      border-top-color: #fff;
      border-radius: 50%;
      animation: credits-spin 0.7s linear infinite;
    }
    #credits-buy-btn.loading .credits-buy-btn-spinner { display: block; }
    @keyframes credits-spin { from { transform: translate(-50%, -50%) rotate(0deg); } to { transform: translate(-50%, -50%) rotate(360deg); } }
  </style>
</head>
<body class="hr-dashboard font-sans text-secondary bg-[#F8FAFC] min-h-screen flex flex-col antialiased">
  <%- include('../partials/gtm-noscript') %>
  <header class="border-b border-[#E5E7EB] bg-white shadow-sm">
    <div class="w-full px-4">
      <div class="flex justify-between items-center h-16">
        <div class="flex items-center gap-3">
          <button type="button" id="hr-nav-burger" class="lg:hidden p-2 -ml-2 rounded-lg text-[#6B7280] hover:bg-slate-100 hover:text-[#1F2937] transition-colors" aria-label="მენიუ">
            <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/></svg>
          </button>
          <a href="/dashboard" class="flex items-center">
            <img src="/images/logo.svg" alt="Samushao.ge" class="h-8" />
          </a>
        </div>
        <div class="hidden lg:flex items-center gap-4">
          <span class="text-xs px-3 py-1.5 rounded-full bg-slate-100 text-slate-700 inline-flex items-center gap-2">
            <span class="font-medium">კრედიტები:</span>
            <span><%= (hrUser && hrUser.creditsDisplay != null) ? hrUser.creditsDisplay : '0' %></span>
          </span>
          <a href="/dashboard/credits" class="text-xs px-3 py-1.5 rounded-lg bg-emerald-100 text-emerald-800 font-medium hover:bg-emerald-200 transition-colors">იყიდე კრედიტები</a>
          <span class="text-sm text-[#6B7280]"><%= hrUser.company_name || hrUser.company_identifier %></span>
          <form action="/dashboard/logout" method="POST" class="inline">
            <button type="submit" class="text-sm px-4 py-2 rounded-lg text-[#6B7280] hover:bg-[#F3F4F6] hover:text-[#1F2937] transition-colors">გასვლა</button>
          </form>
        </div>
      </div>
    </div>
  </header>

  <%- include('partials/mobile-top-bar') %>

  <div class="flex flex-1 w-full flex-col lg:flex-row">
    <%- include('partials/sidebar', { active: 'credits' }) %>

    <main class="flex-1 p-4 sm:p-6 lg:p-8 bg-[#F8FAFC] overflow-auto w-full min-w-0">
      <div class="w-full max-w-2xl mx-auto">
        <h1 class="text-2xl font-semibold text-[#0F172A] tracking-tight mb-2">კრედიტების ყიდვა</h1>
        <p class="text-[#64748B] mb-8">აირჩიეთ კრედიტების პაკეტი ან შეიყვანეთ საკუთარი რაოდენობა.</p>

        <div class="space-y-4" id="credits-options">
          <div role="button" tabindex="0" class="credits-option block w-full bg-white rounded-xl border border-slate-200 shadow-sm p-5 hover:border-[#315EFF] hover:shadow-md transition-all text-left" data-credits="10" data-price="500">
            <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-2">
              <span class="font-medium text-[#0F172A]">10 კრედიტი</span>
              <span class="text-lg font-semibold text-[#0F172A]">500 ₾</span>
            </div>
          </div>
          <div role="button" tabindex="0" class="credits-option block w-full bg-white rounded-xl border border-slate-200 shadow-sm p-5 hover:border-[#315EFF] hover:shadow-md transition-all text-left" data-credits="20" data-price="900">
            <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-2">
              <span class="font-medium text-[#0F172A]">20 კრედიტი <span class="text-emerald-600 text-sm font-normal">(2 საჩუქრად)</span></span>
              <span class="text-lg font-semibold text-[#0F172A]">900 ₾</span>
            </div>
          </div>
          <div role="button" tabindex="0" class="credits-option block w-full bg-white rounded-xl border border-slate-200 shadow-sm p-5 hover:border-[#315EFF] hover:shadow-md transition-all text-left" data-credits="30" data-price="1400">
            <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-2">
              <span class="font-medium text-[#0F172A]">30 კრედიტი <span class="text-emerald-600 text-sm font-normal">(2 საჩუქრად)</span></span>
              <span class="text-lg font-semibold text-[#0F172A]">1 400 ₾</span>
            </div>
          </div>
          <div class="bg-white rounded-xl border border-slate-200 shadow-sm p-5">
            <label for="credits-custom" class="block font-medium text-[#0F172A] mb-3">ან მიუთითეთ რაოდენობა</label>
            <div class="flex flex-col sm:flex-row sm:gap-3 sm:items-center gap-2">
              <input type="number" id="credits-custom" min="1" max="999" placeholder="კრედიტების რაოდენობა" class="flex-1 min-w-[140px] px-4 py-3 text-[15px] border border-slate-200 rounded-xl focus:outline-none focus:ring-2 focus:ring-[#315EFF] focus:border-transparent" />
              <span class="text-[#6B7280]">=</span>
              <span id="credits-custom-price" class="font-semibold text-[#0F172A]">0 ₾</span>
            </div>
            <p class="mt-2 text-xs text-[#6B7280]" id="credits-custom-hint">1 კრედიტი = 50 ₾. 20 და მეტი კრედიტზე 2 საჩუქრად.</p>
          </div>
        </div>

        <button type="button" id="credits-buy-btn" class="mt-6 px-6 py-3 rounded-lg bg-[#315EFF] text-white hover:bg-[#2a4fd9] font-medium disabled:opacity-50 disabled:cursor-not-allowed cursor-pointer" disabled>
          <span class="credits-buy-btn-text">ყიდვა</span>
          <span class="credits-buy-btn-spinner" aria-hidden="true"></span>
        </button>
        <div id="credits-success-msg" class="hidden mt-4 p-4 rounded-xl bg-emerald-50 border border-emerald-200 text-emerald-800 font-medium">ინვოისს გამოგიგზავნით რამდენიმე წუთში თქვენს მეილზე (<%= (hrUser && hrUser.email) ? hrUser.email : '' %>).</div>

      </div>
    </main>
  </div>
  <script>
    (function() {
      var customInput = document.getElementById('credits-custom');
      var customPrice = document.getElementById('credits-custom-price');
      var buyBtn = document.getElementById('credits-buy-btn');
      var successMsg = document.getElementById('credits-success-msg');
      var optionsContainer = document.getElementById('credits-options');
      var selectedPreset = null;

      function customPriceForCredits(n) {
        if (n <= 0) return 0;
        if (n >= 20) return (n - 2) * 50;
        return n * 50;
      }
      function updateCustomPrice() {
        var n = parseInt(customInput.value, 10) || 0;
        var price = customPriceForCredits(n);
        customPrice.textContent = price + ' ₾';
        if (n >= 1) {
          selectedPreset = null;
          optionsContainer.querySelectorAll('.credits-option').forEach(function(el) { el.classList.remove('credits-option-selected'); });
        }
        updateBuyButton();
      }
      function updateBuyButton() {
        var customVal = parseInt(customInput.value, 10) || 0;
        var hasSelection = selectedPreset !== null || customVal >= 1;
        buyBtn.disabled = !hasSelection;
      }
      function selectPreset(el) {
        optionsContainer.querySelectorAll('.credits-option').forEach(function(o) { o.classList.remove('credits-option-selected'); });
        el.classList.add('credits-option-selected');
        selectedPreset = { credits: parseInt(el.getAttribute('data-credits'), 10), price: el.getAttribute('data-price') };
        customInput.value = '';
        customPrice.textContent = '0 ₾';
        updateBuyButton();
      }
      optionsContainer.querySelectorAll('.credits-option').forEach(function(el) {
        el.addEventListener('click', function() { selectPreset(el); });
        el.addEventListener('keydown', function(e) { if (e.key === 'Enter' || e.key === ' ') { e.preventDefault(); selectPreset(el); } });
      });
      customInput.addEventListener('input', updateCustomPrice);
      customInput.addEventListener('change', updateCustomPrice);
      buyBtn.addEventListener('click', function() {
        var credits = selectedPreset ? selectedPreset.credits : (parseInt(customInput.value, 10) || 0);
        if (credits < 1) return;
        buyBtn.disabled = true;
        buyBtn.classList.add('loading');
        fetch('/dashboard/credits/request', {
          method: 'POST',
          headers: { 'Content-Type': 'application/json' },
          body: JSON.stringify({ credits: credits }),
          credentials: 'same-origin'
        })
        .then(function(r) { return r.json().then(function(d) { return { ok: r.ok, data: d }; }); })
        .then(function(r) {
          buyBtn.disabled = false;
          buyBtn.classList.remove('loading');
          if (r.ok && r.data && r.data.ok) {
            successMsg.classList.remove('hidden');
            successMsg.scrollIntoView({ behavior: 'smooth', block: 'nearest' });
          } else {
            alert(r.data && r.data.error ? r.data.error : 'შეცდომა მოხდა.');
          }
        })
        .catch(function() {
          buyBtn.disabled = false;
          buyBtn.classList.remove('loading');
          alert('შეცდომა მოხდა.');
        });
      });
      updateBuyButton();
    })();
  </script>
</body>
</html>
