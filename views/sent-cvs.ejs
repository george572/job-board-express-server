<!DOCTYPE html>
<html lang="ka">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <%- include('partials/head-meta', { seo }) %>
  <link rel="stylesheet" href="/css/style.css" />
</head>

<body class="font-markgeo text-secondary bg-[#F8F9FA]">
  <%- include('partials/header') %>

  <main class="w-full max-w-4xl mx-auto px-4 py-8">
    <h1 class="text-2xl font-medium text-[#151515] mb-6 flex items-center gap-2">
      <%- include('partials/icons/document-check.ejs') %>
      გაგზავნილი CV-ები
    </h1>

    <div class="w-full relative mx-auto" id="jobs-container">
      <% if (jobs && jobs.length > 0) { %>
        <% jobs.forEach(job => { %>
          <%- include('partials/jobItem.ejs', { job, slugify }) %>
        <% }) %>
      <% } else { %>
        <div class="text-center py-12 border border-[#EBEBEB] rounded-lg bg-white">
          <p class="text-gray-500">ჯერ არც ერთ ვაკანსიაზე არ გაგიგზავნიათ CV.</p>
          <a href="/" class="inline-block mt-4 text-primary font-medium hover:underline">ვაკანსიების ნახვა</a>
        </div>
      <% } %>
    </div>
  </main>
</body>

</html>
