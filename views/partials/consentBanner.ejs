<div class="fixed inset-x-0 bottom-0 z-40 px-4 pb-4">
  <div class="mx-auto max-w-4xl rounded-2xl bg-white shadow-lg border border-gray-200 px-4 py-3 sm:px-6 sm:py-4 flex flex-col sm:flex-row sm:items-center sm:justify-between gap-3">
    <div class="text-sm text-gray-700">
      <p class="font-medium text-[#151515] mb-1">
        ვიყენებთ cookies და ვამუშავებთ თქვენს პირად მონაცემებს უკეთესი გამოცდილებისთვის.
      </p>
      <p class="text-xs text-gray-500">
        დეტალებისთვის გაეცანით
        <a href="/privacy-policy" class="underline text-primary hover:text-primary/80">მონაცემთა დაცვის პოლიტიკას</a>.
      </p>
    </div>
    <form method="post" action="/consent/accept" class="flex items-center gap-2 justify-end">
      <input type="hidden" name="redirect" value="<%= (seo && seo.canonical) ? seo.canonical : req?.originalUrl || '/' %>" />
      <button
        type="submit"
        class="inline-flex items-center justify-center rounded-full bg-primary text-white text-sm font-medium px-4 py-2 shadow-sm hover:bg-primary/90 transition">
        ვეთანხმები
      </button>
    </form>
  </div>
</div>

