<div id="cvAiBanner" class="z-40 flex items-center justify-center px-4 py-6" style="display:none; position: fixed; top: 0; left: 0; right: 0; bottom: 0; width: 100vw; height: 100vh; min-height: 100vh; background: rgba(0,0,0,0.45);">
  <div class="w-full max-w-2xl max-h-[85vh] overflow-y-auto rounded-2xl bg-white shadow-lg border border-gray-200 px-4 py-4 sm:px-6 sm:py-5">
    <div class="flex flex-col gap-3">
      <p class="text-sm sm:text-base font-medium text-[#151515]">
        თუ რეზიუმე არ გაქვს, ჩვენ მოგიძებნით სამსახურს. რამდენიმე კითხვას დაგისვამთ, რეზიუმეს გაგიკეთებთ ავტომატურად, და შეგეძლებათ უამრავ ვაკანსიაზე გაგზავნოთ.
      </p>
      <p class="text-sm sm:text-base text-[#4B5563]">
        ეს დაგეხმარებათ უფრო მარტივად იპოვოთ სამსახური, და ჩვენც შევძლებთ თქვენი რეზიუმე დამსაქმებლებს გავუგზავნოთ,  გნებავთ?
      </p>
      <div class="flex justify-end gap-2 pt-2">
        <button
          type="button"
          id="cvAiBannerNo"
          class="px-4 py-2 text-sm border border-gray-300 rounded-lg text-gray-700 hover:bg-gray-50 cursor-pointer"
        >
          არა
        </button>
        <button
          type="button"
          id="cvAiBannerYes"
          class="px-4 py-2 text-sm bg-[#315EFF] text-white rounded-lg hover:opacity-90 cursor-pointer"
        >
          კი
        </button>
      </div>
    </div>
  </div>
</div>
<script>
(function() {
  var banner = document.getElementById('cvAiBanner');
  if (!banner) return;

  var COOKIE_NAME = 'cv_ai_banner_seen';

  function hasSeenBanner() {
    return document.cookie.indexOf(COOKIE_NAME + '=1') !== -1;
  }

  function setSeenCookie() {
    var days = 365;
    var date = new Date();
    date.setTime(date.getTime() + days * 24 * 60 * 60 * 1000);
    document.cookie = COOKIE_NAME + '=1; path=/; expires=' + date.toUTCString();
  }

  if (window.location.pathname === '/' && !hasSeenBanner()) {
    banner.style.display = 'flex';
  }

  var yesBtn = document.getElementById('cvAiBannerYes');
  var noBtn = document.getElementById('cvAiBannerNo');

  if (yesBtn) {
    yesBtn.addEventListener('click', function() {
      setSeenCookie();
      window.location.href = '/sheqmeni-cv';
    });
  }

  if (noBtn) {
    noBtn.addEventListener('click', function() {
      setSeenCookie();
      banner.style.display = 'none';
    });
  }
})();
</script>
